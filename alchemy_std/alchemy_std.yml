

units:
    alchemy_std.ssh_units:
        Init ssh connection:
            func: init_ssh_connection
        SSH command:
            func: ssh_runcmd

    alchemy_std.nfs_units:
        NFS mount remote:
            func: nfs_mount_remote

